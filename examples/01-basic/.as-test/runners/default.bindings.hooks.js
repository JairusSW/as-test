export function createUserImports(_ctx) {
  return {
    // env: {
    //   now_ms: () => Date.now(),
    // },
  };
}

export async function runModule(_exports, _ctx) {
  // The generated bindings helper already calls exports._start().
  // Add extra startup calls here when your module exposes them.
  //
  // Example:
  // _exports.run?.();
}
